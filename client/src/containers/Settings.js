import CryptosAutoComplete from "../components/CryotosAutoComplete";

const Settings = () => {
    return (
        <div>
            <h2>Settings Page</h2>
            <CryptosAutoComplete />
        </div>
    );
}

export default Settings;